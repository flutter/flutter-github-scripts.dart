name: dartdev_github_scripts
description: Scripts to provide reports on dart.dev's repo.
version: 0.0.1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  args: ^1.5.0
  csv:  ^4.0.0
  quiver: '>=2.0.0 < 3.0.0'
  glob: ^1.0.3
  path:
  lcov:
  graphql: ^2.0.0
  columnar_output:
    git: https://github.com/kf6gpe/columnar-output.dart.git

dev_dependencies:
  test: ^1.0.0
  pedantic:
